import './header.css';

const Header = () => {
    return (
        <div className="header">
            <a href="#">Home</a>
            <a href="#">New Feed</a>
            <a href="#">Messages</a>
        </div>
    );
};

export default Header;
